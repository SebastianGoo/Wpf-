<Window x:Class="gridview分组.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gridview分组"
        mc:Ignorable="d"        
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"

        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <telerik:RadGridView x:Name="RadGridView" 
                             AutoGenerateColumns="False" 
                             ShowGroupPanel="False" 
                             ItemsSource="{Binding Data}" telerik:StyleManager.Theme="Windows11">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Name" DataMemberBinding="{Binding Name}" />
                <telerik:GridViewDataColumn Header="Category" DataMemberBinding="{Binding Category}" />
                <telerik:GridViewDataColumn Header="Price" DataMemberBinding="{Binding Price}" />
            </telerik:RadGridView.Columns>
            <telerik:RadGridView.GroupDescriptors>
                <telerik:GroupDescriptor Member="Category" SortDirection="Ascending" />
            </telerik:RadGridView.GroupDescriptors>
        </telerik:RadGridView>
    </Grid>
</Window>
